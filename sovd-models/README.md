# sovd-models

Core SOVD (ISO 17978) data structures and types.

## Features

| Feature | Default | Description |
|---------|---------|-------------|
| `jsonschema` | No | JSON schema support |
| `jsonschema-schemars` | Yes | JSON schema generation with schemars |
