# sovd

Core SOVD (ISO 17978) data structures and types.

## Overview

This crate provides the fundamental data structures for SOVD implementation.

## Usage

Add this to your `Cargo.toml`:

```toml
[dependencies]
sovd = "0.0.1"
```
